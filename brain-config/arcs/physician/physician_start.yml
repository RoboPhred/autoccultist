name: "Physician: Startup"
legacy: physician
selectionHint:
  anyOf:
    - elementId: legacyphysicianjob
motivations:
  - name: Begin the Game
    primaryGoals:
      - !import /goals/legacy/physician/work-institute.yml
      - !import /goals/legacy/physician/study-notes.yml
      - !import /goals/discover-bookdealer.yml
    secondaryGoals:
      - !import /goals/survive.yml
